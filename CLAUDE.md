# CLAUDE.md — 最上位ルール

このファイルはプロジェクトの最上位ルール。毎セッション読み込まれるため、
**常に必要なことだけ**を書く。一度しか使わない手順は `.claude/commands/` に置く。

---

## セッション開始時

以下の順で状態を確認してから作業を始める。

1. `docs/concept.md` が存在するか確認する
   - **存在しない** → まだ方向性が固まっていない。`/kickoff` を実行してヒアリングから始める
   - **存在する** → 2 へ進む
2. `docs/architecture.md` を読み、技術スタックと構造を把握する
3. `docs/backlog.md` と `docs/known-issues.md` に目を通す
4. `work/` の最新ディレクトリを確認し、未完了の作業がないか確認する

---

## 開発フロー

```
ヒアリング → ドキュメント承認 → 作業メモ作成 → 実装 → 完了処理
```

この順序を崩さない。**承認前にコードを書き始めてはならない。**

### プロジェクト開始時（`docs/concept.md` がない場合）

`/kickoff` コマンドでヒアリングを行い、以下の順に **1ファイルずつ**草案を示して承認を得る。

```
1. docs/concept.md      草案提示 → 承認
2. docs/architecture.md 草案提示 → 承認
3. work/ の作業メモ作成 → 承認
4. 実装開始
```

### 通常の作業開始時

`work/YYYYMMDD-HHMM-タイトル/` を作成し、以下を **1ファイルずつ**承認を得ながら作る。

```
1. 1_requirements.md 作成 → 承認
2. 2_design.md       作成 → 承認（複雑な作業のみ・任意）
3. 3_tasks.md        作成 → 承認
4. 実装開始
```

作業開始前に `docs/backlog.md` と `docs/known-issues.md` を確認し、
関連する項目があれば「今回あわせて対応しますか？」と提案する。

`3_tasks.md` の最後には必ず `- [ ] ラップアップ（`/wrapup` を実行する）` を
タスクとして加える。実装タスクを消し込んだだけで終わらせず、
締め処理を忘れないようにするため。

### 作業完了時

`/wrapup` コマンドで締め処理を行う。概要は以下（詳細は `.claude/commands/wrapup.md`）。

1. 品質チェック（テスト・型チェック・lint）を実行する
2. `1_requirements.md` の受け入れ条件を確認する
3. `3_tasks.md` のタスクをすべて `- [x]` にする（ファイルは削除せず残す）
4. 積み残しを `backlog.md` / `known-issues.md` に振り分ける
5. `docs/` への反映が必要な決定事項を報告し、**承認を得てから**更新する

**エラーが残ったまま、または受け入れ条件を満たさないまま「完了」と報告しない。**

---

## ディレクトリ構成

```
リポジトリルート/
├── CLAUDE.md               # 永続：最上位ルール（このファイル）
├── README.md               # 人間向け：これは何か・動かし方
├── .claude/
│   ├── settings.json       # 権限・フック（チーム共有・git 管理下）
│   ├── settings.local.json # 個人設定（git 管理外）
│   └── commands/           # プロジェクト固有のスラッシュコマンド
├── docs/                   # 永続ドキュメント
│   ├── concept.md          # 何を・誰のために作るか（方向性）
│   ├── architecture.md     # 技術スタック・構造の決定事項
│   ├── conventions.md      # 命名・コード規約
│   ├── backlog.md          # やりたいことの置き場（承認不要）
│   ├── known-issues.md     # 不具合・技術的負債（承認不要）
│   └── decisions/          # ADR：技術決定の履歴
│       └── NNNN-title.md
├── work/                   # 作業メモ（作業単位・完了後も残す）
│   └── YYYYMMDD-HHMM-タイトル/
│       ├── 1_requirements.md
│       ├── 2_design.md     # 任意：複雑な作業のみ
│       └── 3_tasks.md      # 完了後も残す（タスクは全て [x] にする）
├── import/                 # 外部から取り込む資料（読み取り専用）
├── src/                    # ソースコード
├── tests/                  # テスト
└── scripts/                # 補助スクリプト
```

### `docs/` と `work/` の違い

|          | `docs/`          | `work/`        |
|----------|------------------|----------------|
| 寿命     | プロジェクト全体 | 1作業単位      |
| 書く時期 | 決定したとき     | 作業開始前     |
| 内容     | What・Why        | Now・How       |
| 役割     | 記憶・制約       | 今日の指示書   |

---

## ドキュメント管理ルール

### 承認が必要なファイル

`docs/` 以下は **`backlog.md` と `known-issues.md` を除き、承認なしに変更しない。**
変更が必要な場合は内容を報告し、承認を得てから更新する。

`concept.md` の特別ルール：プロダクトの方向性を変える変更は、
新規ファイルで議論・合意してから反映する。

### 承認不要のファイル（例外）

`docs/backlog.md` と `docs/known-issues.md` は **承認なしで追記してよい。**

理由：これらは「今すぐ対応する判断」ではなく「忘れないための記録」であり、
会話を止めるほどの決定ではないため。追記した内容は作業完了時にまとめて報告する。

---

## TODO・課題・不具合の管理

寿命によって書く場所を変える。**迷ったら下表に従う。**

| 見つけたもの                       | 書く場所                      |
|------------------------------------|-------------------------------|
| 今回のスコープ内の作業             | `work/.../3_tasks.md`         |
| 機能のアイデア・あとでやりたいこと | `docs/backlog.md`             |
| 不具合（動作がおかしい）           | `docs/known-issues.md` 種別=バグ |
| 技術的負債（動くが汚い・遅い）     | `docs/known-issues.md` 種別=負債 |

### 重要な原則

- 作業中に思いついたことを `3_tasks.md` に足さない。作業が終わらなくなる。
  スコープ外のものは `backlog.md` か `known-issues.md` に送る
- `backlog.md` の却下項目は削除せず取り消し線で残す。同じ議論を繰り返さないため
- `known-issues.md` は解決しても行を削除せず「なおした日」を埋める。記録として残す
- 課題管理はファイルで一元化する。GitHub Issues とファイルの二重管理はしない

---

## 開発ルール

### 基本姿勢

- 不明確な点があれば実装を止めて確認する。**推測で進めない**
- 既存コードのスタイル（命名・コメント量・イディオム）に合わせる
- スコープを勝手に広げない。気づいた改善点は `backlog.md` に送る

### 品質チェック

実装後、以下を実行して結果を報告する（存在するもののみ）。

- テスト
- 型チェック
- lint / formatter

コマンドが未定義の場合は `docs/architecture.md` に記載する。
**エラーが残ったまま「完了」と報告しない。**

### git 運用

- **コミット・push はユーザーから指示があったときのみ行う**
- コミット粒度は「1つの意味のある変更」単位。作業メモ1つ＝1コミットとは限らない
- デフォルトブランチで作業している場合、コミット前にブランチを切る
- `--no-verify` などフックの回避はしない

### 禁止事項

- 秘密情報（APIキー・パスワード・トークン）をコードやドキュメントに書かない。
  環境変数または `.env`（git 管理外）を使う
- `import/` 以下を書き換えない（読み取り専用）
- 破壊的操作（`git reset --hard` / 大量ファイル削除 / DB のデータ削除）は
  実行前に必ず確認を取る
- 動作確認していないものを「動作します」と報告しない

---

## 参照

| 知りたいこと           | 読むファイル              |
|------------------------|---------------------------|
| 何を作っているか       | `docs/concept.md`         |
| 技術スタック・構造     | `docs/architecture.md`    |
| コード規約             | `docs/conventions.md`     |
| なぜその技術を選んだか | `docs/decisions/`         |
| 未対応の課題           | `docs/known-issues.md`    |
| やりたいことリスト     | `docs/backlog.md`         |

## スラッシュコマンド

| コマンド  | 用途                                             |
|-----------|--------------------------------------------------|
| `/kickoff` | プロジェクト開始時のヒアリング → concept / architecture 作成 |
| `/wrapup`  | 作業完了時の締め処理                             |
