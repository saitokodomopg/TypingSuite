# 3. Tasks — 練習画面UI改善3点

完了したら `- [x]` に消し込む。**全部終わってもファイルは削除せず残す。**

作業中に思いついたスコープ外のことはここに足さない。
`docs/backlog.md` か `docs/known-issues.md` に送る。

- [x] 課題選択をボタン化する
      - `PracticeScreen.tsx` の `<select>` を、`tasks` を `.map` したボタン群に置き換える
      - 選択中の課題が見た目で分かるようにする（例: `aria-pressed` + スタイル）
      - `PracticeScreen.test.tsx` の「セレクトボックスで課題を切り替えられる」テストを
        ボタン操作（`getByRole('button', { name: ... })`）に書き換える
- [x] 入力欄で入力済み/未入力の文字を区別する
      - 当初「残り未入力も薄い色で表示」で実装したが、ユーザー確認の結果
        「次の1文字だけグレー表示、その先は非表示のまま」に変更（既存の
        `displayText` で全文は別途見えているため、進捗表示は最小限でよいという判断）
      - 表示は「入力済み（通常色）」「次の1文字（グレー）」の2区分。
        未入力の残りは進捗表示エリアには出さない
      - 色はCSSクラス（`src/index.css` の `.current-text`）で表現。インラインstyle直書きはしない
- [x] リトライボタンを追加する
      - 練習エリア付近に「リトライ」ボタンを追加する
      - 押すと同じ課題（同じ出題文）を維持したまま、入力進捗・ミス表示・結果表示をリセットする
        （`task.generate()` で新しい問題を引き直すのではなく、今表示中の `challenge` をそのまま使う）
      - 入力途中・結果表示中のどちらの状態からでも押せることを確認する
      - リトライ操作のテストを追加する
- [x] `npm run dev` を起動し、3点とも実際のブラウザ操作で確認する（ユーザーが確認済み）
- [x] `docs/known-issues.md` No.1（`touch-type-dk` ダミー課題）は今回スコープ外のため触らない
      （課題選択のボタン化で見た目には影響するが削除はしない）
- [x] PR を作成し `main` にマージする（PR #3、ブランチは削除済み）
- [x] `main` への反映後、Cloudflare Pages の自動デプロイ完了を本番URLで確認する
      （Workers Builds チェック success。本番JSバンドルに新実装の文字列が
      含まれることを確認。旧実装の `untyped-text` は0件で残っていないことも確認）
- [x] ラップアップ（`/wrapup` を実行する）

## 品質チェック

- [x] テスト（`npm run test` — 26 passed）
- [x] 型チェック（`npm run typecheck` — エラーなし）
- [x] lint（`npm run lint` — エラーなし）
