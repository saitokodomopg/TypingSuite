# Backlog

やりたいこと・アイデアの置き場。**承認不要で追記してよい。**

作業中にスコープ外の思いつきが出たらここに送る。
却下したものは削除せず取り消し線で残す（同じ議論を繰り返さないため）。

## 次にやりたい

- [ ] ゲームモードの候補（`Game` 型が未整備のため、型定義とregistry対応が先に必要）
      - タイムアタック（制限時間でスコア最大化）
      - 落ちてくる単語を打って消す（タワーディフェンス風）
      - 敵を倒すRPG風演出
      - コンボ/連続正解でスコア倍率

## いつかやりたい

- [ ] `README.md` の「動かし方」がひな型のプレースホルダのまま未記入。
      今回、開発機に Node.js/npm が入っておらず `npm install` 前に別途インストールが必要だった。
      新しく参加する生徒向けに、セットアップ手順（Node.jsのインストール含む）を明記すると親切

- [ ] ローマ字変換（`src/core/engine/romaji.ts`）が長音（ー）・外来語のカタカナ表記に未対応。
      現状のひらがな課題文では困らないが、ローマ字課題の中身を作る際に必要なら対応する
- [ ] 生徒の課題選択が重複しない仕組み。現状は`src/tasks/README.md`上で
      各自が名前を書き添える自己申告のみ。人数が増えて回りにくくなったら検討する

- [ ] ホームポジション練習：苦手キー（ミスの多いキー）を多めに出す適応出題。
      記録の保存（Supabase 連携）が先に必要。2026-09-26 ホームポジション練習の強化でスコープ外にした
- [ ] ホームポジション練習：始める段階を選べるようにする（今は毎回段階1から）。
      慣れた生徒が毎回 F・J から始めるのは退屈になりうる
- [ ] 時間制モード（`TypingTask.endless`）を他の課題にも使う

## 却下・保留

<!-- 例: - ~~リアルタイム対戦~~ — 2026-09-19 スコープ外と判断 -->
