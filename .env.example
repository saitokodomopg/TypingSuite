# 環境変数のテンプレート。
# このファイルをコピーして .env を作る（.env は git 管理外）。
#
#   cp .env.example .env
#
# 秘密情報（APIキー・パスワード・トークン）は .env にのみ書き、
# コードやドキュメントには絶対に書かない。

# 例:
# API_KEY=
# DATABASE_URL=

# Supabase（値は Project Settings > API、または Connect ボタンから取得する）
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
